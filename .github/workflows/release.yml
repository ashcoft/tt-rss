name: Changesets
on:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: write
  pull-requests: write

uses: changesets/action@v1.9.0
with:
  title: "Release {{version}}"
  commitMessage: "chore: release version {version}"
  setupGitHubUser: true